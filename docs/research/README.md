# Historical research index

Everything in this directory is a dated research snapshot. It may contain old repository counts, proposed technology choices, superseded priorities and “next step” language that no longer describes the project.

Rules for humans and Agents:

- Do not derive the active roadmap from this directory.
- Verify implementation claims against current code and `docs/first-acceptance-status.zh-CN.md`.
- A recommendation is active only when adopted by a current ADR, `docs/architecture.md` or `docs/development-plan.zh-CN.md`.
- Preserve source links and historical reasoning; update canonical documents instead of rewriting history to look current.

Current routing is in `docs/current-state.json`.
